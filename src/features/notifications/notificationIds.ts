/** Stable identifiers for scheduled local notifications (idempotency + cancel). */

export const REENGAGEMENT_REMINDER_ID = 'notifications.reengagement.24h';

/** Example / dev-only scheduled notification (see `scheduleExampleReminderInSeconds`). */
export const EXAMPLE_LOCAL_NOTIFICATION_ID = 'notifications.example.dev';
